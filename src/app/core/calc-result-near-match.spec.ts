import { CalcResultNearMatch } from './calc-result-near-match';

describe('CalcResultNearPart', () => {
  it('should create an instance', () => {
    expect(new CalcResultNearMatch()).toBeTruthy();
  });
});
